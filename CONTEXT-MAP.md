# Context Map

This repo has multiple domain contexts. Each context has its own `CONTEXT.md` and `docs/adr/` directory.

| Context | Path |
|---------|------|
| Server (Go backend) | `server/` |
| Frontend (TypeScript/React) | `frontend/` |

System-wide ADRs that apply to all contexts live in `docs/adr/` at the root.